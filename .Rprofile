source("renv/activate.R")
# .Rprofile file content

# Use a specific renv cache path
#Sys.setenv(RENV_PATHS_CACHE = "/model/shared_R_libs/")

# Use a specific library path for the project
#Sys.setenv(RENV_PATHS_LIBRARY = "/model/shared_R_libs/project_library")

# Use a specific R version for reproducibility across nodes
#Sys.setenv(RENV_CONFIG_R_VERSION = "4.3.3")
Sys.setenv(LD_LIBRARY_PATH = paste("/usr/local/lib", Sys.getenv("LD_LIBRARY_PATH"), sep = ":"))
